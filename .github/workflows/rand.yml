name: we are tired
 on: [push, pull_request]

jobs:
 build:
  runs-on: ubuntu-latest
  steps:
    -uses: actions/checkout@v3
    -name: Set up rand's python
     uses: actions/setup-python@v4
    with:
        python-version: "3.10"
    -name: Run rand's test
    run: python -m unittest discover
